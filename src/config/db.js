const mongoose=require("mongoose")

function connectToDB(){
    mongoose.connect(process.env.MONGO_URI)




















































































































    
    .then(()=>{
        console.log("MongoDB is connected successfully")
    })
    .catch(err=>{
        console.log("error coonected bto DB")
        process.exit(1)
    })
    }

    module.exports=connectToDB;